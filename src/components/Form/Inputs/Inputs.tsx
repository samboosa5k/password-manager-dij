import styled from 'styled-components';

const Select = styled.select``;
const Option = styled.option``;
const Label = styled.label``;
const Input = styled.input``;

export { Select, Option, Label, Input };
