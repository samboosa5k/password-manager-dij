import styled from 'styled-components';

const UL = styled.ul`
  font-size: 0.75em;
  font-weight: 600;
  margin: 0 auto 0.25em 0;
  line-height: 2;
  color: var(--gray-600);
`;

const LI = styled.li``;

export default UL;
export { LI };
