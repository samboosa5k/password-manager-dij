import styled from 'styled-components';

export const P = styled.p`
  font-size: 0.75em;
  font-weight: normal;
  color: var(--gray-900);
  margin: 0.2em 0;
`;

export const SPAN = styled.span`
  font-size: 1em;
  margin: auto 1rem;
  color: var(--gray-900);
`;
