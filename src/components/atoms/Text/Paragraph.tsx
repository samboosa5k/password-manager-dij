import styled from 'styled-components';

export const P = styled.p`
  font-size: 0.75em;
  font-weight: normal;
  margin: 0;
  color: var(--gray-900);
`;

export const SPAN = styled.span`
  font-size: 1em;
  margin: auto 1rem;
  color: var(--gray-900);
`;
